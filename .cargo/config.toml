# FoundationDB client library path — needed for linking.
# Install FDB: https://github.com/apple/foundationdb/releases
# On macOS ARM64, extract the FoundationDB-*_arm64.pkg client package.
[build]
rustflags = ["-L", "/tmp/fdb_extract/client_contents/usr/local/lib"]
